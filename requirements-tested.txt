# Exact package versions used for the packaged v1.4.5 CPU verification and
# the saved TC-DSFNet five-fold verification run. Python: 3.13.5.
# The installed PyTorch build reports 2.10.0+cpu; use the CPU wheel appropriate
# for your platform when reproducing this environment.
numpy==2.3.5
scipy==1.17.0
torch==2.10.0
shapely==2.1.2
scikit-learn==1.8.0
pytest==9.0.2
